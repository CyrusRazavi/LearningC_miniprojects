#include <stdio.h>

int main(void){
	int itemPrice=3,shippingCost=1, sum;
	sum =itemPrice + shippingCost;
	int value=(itemPrice < 4);
	if(value == 1){
		printf("value is good");
}
	else{
		printf("value is too high");
}
:wq
}
